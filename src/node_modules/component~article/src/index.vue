<style lang="less">
  @import (reference) "src/styles/global";
  @import "src/styles/global/utilities";
  @import "../styles/index";
</style>

<template>
  <div class="c-article">
    <img class="c-article__thumbnail" :src="article.PictureUrl" alt="{{ article.Title }}">
    <div class="c-article__body">
      <h2 class="c-article__title">{{ article.Title }}</h2>

      <ul class="list-unstyled">
        <template v-if="article.IsAuction">
          <li class="hidden-xs hidden-ms">{{ article.BidsCount }} bids</li>
          <li class="u-color--conversion-auction u-text-bold">CHF {{ article.Price }}</li>
        </template>
        <template v-if="article.IsBuyNow">
          <li class="hidden-xs hidden-ms">Direct buy</li>
          <li class="u-color--conversion-buynow u-text-bold">CHF {{ article.BuyNowPrice }}</li>
        </template>
        <template v-if="! (article.IsAuction || article.IsBuyNow)">
          <li class="u-color--conversion-classified u-text-bold">CHF {{ article.Price }}</li>
        </template>
      </ul>
    </div>
    <a class="c-article__link" :href="article.Url" title="{{ article.Title }}"></a>
  </div>
</template>

<script>
  module.exports = {
    props: {
      article: Object
    }
  }
</script>
